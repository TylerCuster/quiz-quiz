'use strict';

/* jasmine specs for controllers go here */

describe('controllers', function(){
  beforeEach(angular.mock.module('myApp.controllers'));

  it('should have a QuizController controller', function() {
  	1 + 2 == 3;
  });

  it('should ....', inject(function() {
    //spec body
  }));

  it('should ....', inject(function() {
    //spec body
  }));
});